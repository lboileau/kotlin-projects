# Common Library — AI Context

## Overview
Shared utilities, types, and helpers with no I/O dependencies.

## Package
`com.example.libs.common`

## Public API
- `logger()` — Inline reified function to create SLF4J Logger instances with minimal boilerplate

## Gradle Module
`:libs:common`
