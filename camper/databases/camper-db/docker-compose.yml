version: "3.8"
services:
  camper-db:
    image: postgres:16-alpine
    container_name: camper-db
    environment:
      POSTGRES_DB: camper_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - camper-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  camper-db-data:
